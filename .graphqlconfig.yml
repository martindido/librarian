projects:
  librarian:
    schemaPath: src/graphql/schema.json
    includes:
      - src/App/graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/App/types/GraphQL.ts
        docsFilePath: src/graphql
